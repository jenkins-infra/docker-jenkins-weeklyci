buildDockerAndPublishImage([
    imageName: 'jenkins-weekly',
    rebuildImageOnPeriodicJob: false, // We update core at least once a week
    buildDockerConfig: [
        targetplatforms: 'linux/amd64,linux/arm64'
    ],
])
